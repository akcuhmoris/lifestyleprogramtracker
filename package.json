{
  "name": "lifestyle-program-tracker",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@program/web",
    "build": "npm run build --workspace=@program/web",
    "start": "npm run start --workspace=@program/web",
    "lint": "npm run lint --workspace=@program/web"
  }
}
